{
  "name": "bot-admin-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Optional React/Vite SPA for the Bot Admin Panel — same REST+SSE API as the built-in dashboard.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.0"
  }
}
